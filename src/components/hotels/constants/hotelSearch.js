// LocalStorage keys
export const HOTEL_SEARCH_STORAGE_KEY = "flymoon_hotel_search_data";

// Default values
export const DEFAULT_GUESTS = {
  adults: 1,
  children: 0,
  rooms: 1,
};

// Maximum values
export const MAX_ROOMS = 6;
export const MAX_GUESTS_PER_ROOM = 6;

// Debounce delays (in milliseconds)
export const SEARCH_DEBOUNCE_DELAY = 300;

// Minimum characters for search
export const MIN_SEARCH_CHARACTERS = 2;

// Toast duration (in milliseconds)
export const TOAST_DURATION = 3500;

// Search config
export const SEARCH_CONFIG = {
  currency: "SAR",
  culture: "en-US",
  channelId: "b2bSaudi",
  affiliateRegion: "B2B_Saudi",
  segmentId: "NewRevamp",
  companyId: "4064",
  gstPercentage: 0,
  tdsPercentage: 0,
  defaultNationality: "SR",
  defaultCountryCode: "SR",
};

// Child age range
export const CHILD_AGE_RANGE = {
  min: 1,
  max: 12,
};

